import { DateOnlyInputDirective } from './date-only-input.directive';

describe('DateOnlyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DateOnlyInputDirective();
    expect(directive).toBeTruthy();
  });
});
